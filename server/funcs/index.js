const createQueue = require('./createQueue');
const setNextUrl = require('./setNextUrl');
const findPath = require('./findPath');
const generateOutput = require('./generateOutput');

module.exports = {
    helperFuncs: {
        createQueue,
        setNextUrl,
        findPath,
        generateOutput
    }
}